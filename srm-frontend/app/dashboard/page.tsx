'use client';

export default function DashboardRoot() {
  // Справжній редирект за роллю відбувається автоматично у layout.tsx
  // Ця сторінка працює лише як візуальна заглушка під час аналізу прав доступу.
  return <div className="flex items-center justify-center min-h-screen">Аналіз прав доступу...</div>;
}